# Функція для перевірки правильності вводу числа
def get_sections_count():
    while True:
        try:
            count = int(input("Введіть кількість розділів (1–5): "))
            if count <= 0:
                print("Помилка: кількість має бути додатним числом.")
            elif count > 5:
                print("Забагато! Максимум дозволено 5 розділів.")
            else:
                return count
        except ValueError:
            print("Помилка: потрібно ввести ціле число без дробової частини.")


# --- Основна програма ---
print("Програма сортування назв розділів документа\n")

# 1. Отримати кількість розділів від користувача
n = get_sections_count()

# 2. Обрати метод сортування
print("\nОберіть метод сортування:")
print("1 — За зростанням (А → Я)")
print("2 — За спаданням (Я → А)")
print("3 — За довжиною назви")

method = input("Ваш вибір (1/2/3): ")

# 3. Створити порожній список для назв розділів
sections = []

# 4. Запитати назви розділів у циклі
for i in range(n):
    title = input(f"Введіть назву розділу №{i + 1}: ").strip()
    sections.append(title)

# 5. Прибрати дублікати (перетворення на множину, потім назад у список)
sections = list(set(sections))

# 6. Відсортувати список відповідно до вибору користувача
if method == "1":
    sections.sort()  # за алфавітом
elif method == "2":
    sections.sort(reverse=True)  # зворотній порядок
elif method == "3":
    sections.sort(key=len)  # за довжиною
else:
    print("Невірний вибір методу сортування — використовується стандартне сортування (А→Я).")
    sections.sort()

# 7. Вивести результат
print("Відсортовані розділи:")
for i, title in enumerate(sections, start=1):
    print(f"{i}. {title}")
